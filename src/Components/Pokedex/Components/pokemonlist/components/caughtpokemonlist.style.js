import { makeStyles } from "@material-ui/core/styles";

export const CaughtPokemonListStyle = makeStyles((theme) => ({

List: {
    color: 'white',
    background: '#505050',
    width: '300px',
    maxWidth: 360,
    position: 'relative',
    overflow: 'auto',
    height: '730px',
    maxHeight: 730,
    marginTop: '130px',
}

}));